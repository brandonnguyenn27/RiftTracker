import {Progress} from "@nextui-org/react";

const DamageContainer = ({player, puuid, maxDamage}) => {
    
    return (
        <div>
            <Progress 
            aria-label="damage" 
            
            />
        </div>
    );
}